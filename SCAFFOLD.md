# Scaffold trail

Generated by `@bitstone/create-launcher` on 2026-07-12T14:42:31.153Z.
Base launcher: `/Users/yudonghun/MinecraftProjects/blue-zone-launcher`

## Answers
```json
{
  "launcherId": "king_war-launcher",
  "productName": "King War Launcher",
  "appId": "kingwarlauncher",
  "description": "킹워 동맹전 (King War) 마인크래프트 런처",
  "author": "donghune",
  "homepage": "https://github.com/donghune/king_war-launcher",
  "githubUser": "donghune",
  "githubRepo": "king_war-launcher",
  "copyrightYear": 2026,
  "server": {
    "id": "king_war-main",
    "name": "킹워 동맹전 서버",
    "description": "킹워 동맹전 (King War) - Fabric 1.21.4",
    "address": "game.donghune.kr:25571",
    "minecraftVersion": "1.21.4",
    "icon": "https://raw.githubusercontent.com/donghune/king_war-launcher/master/app/assets/images/SealCircle.png"
  },
  "modulePresets": [
    "fabric-1.21.4",
    "fabric-api-0.119.4+1.21.4",
    "fabric-language-kotlin-1.13.11",
    "sodium-iris"
  ],
  "extraModules": [
    {
      "id": "kr.donghune:king_war_client:1.0-SNAPSHOT",
      "name": "King War Mod",
      "type": "FabricMod",
      "artifact": {
        "size": 120660,
        "MD5": "a33fc704b2e756ccccd4403ffe90b3b7",
        "url": "https://github.com/donghune/king_war-launcher/releases/download/v1.0.0/king_war_client-1.0-SNAPSHOT.jar"
      }
    }
  ]
}
```

## Next steps
1. Replace branding assets under `app/assets/images/` (logo, SealCircle.png) so they match `King War Launcher`.
2. Review `distribution.json` — mod jars referenced via GitHub Releases need real `size`/`MD5`.
3. Inspect the residuals listed below and edit by hand where needed.
4. `npm install && npm start` to dev-launch.

## Residuals from the base launcher (review by hand)

- `app/assets/lang/_custom.toml` — Blue Zone (literal)
- `app/assets/lang/_custom.toml` — BLUE ZONE (uppercase)
- `app/assets/lang/ko_KR.toml` — Blue Zone (literal)

Notes:
- Game-specific lines (e.g. `welcomeHeader`, `welcomeDescription`, gameplay strings in `app/assets/lang/*.toml`) are intentionally left intact — they describe the *event*, not the launcher, so they need a manual rewrite for each new event.
- Shader/file URLs in `distribution.json` still point at the base launcher's GitHub Release. After uploading the same assets to `donghune/king_war-launcher` releases, update those URLs.
